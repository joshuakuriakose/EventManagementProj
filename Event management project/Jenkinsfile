pipeline {
    agent { label 'DESKTOP-PNDAH72' }

    environment {
        XAMPP_HTDOCS = 'C:\\xampp\\htdocs\\EventProject'
        PROJECT_SOURCE = "${WORKSPACE}"  // or "${WORKSPACE}\\EventProject" if needed
    }

    stages {
        stage('Checkout SCM') {
            steps {
                echo '📥 Checking out source code from Git...'
                checkout scm
            }
        }

        stage('Start Apache Server') {
            steps {
                echo '🚀 Starting Apache Server if not already running...'
                bat '''
                tasklist /FI "IMAGENAME eq httpd.exe" | find /I "httpd.exe" >nul
                if errorlevel 1 (
                    echo Apache not running. Starting now...
                    start "" "C:\\xampp\\apache\\bin\\httpd.exe"
                    timeout /T 5 >nul
                ) else (
                    echo Apache is already running.
                )
                '''
            }
        }

        stage('PHP Lint Check') {
            steps {
                echo '🔍 Running PHP lint check on project files...'
                bat '''
                for %%f in ("%PROJECT_SOURCE%\\*.php") do (
                    php -l "%%f"
                    if errorlevel 1 exit /b 1
                )
                '''
            }
        }

        stage('Deploy to XAMPP') {
            steps {
                echo '📂 Deploying website to XAMPP htdocs...'
                bat '''
                if exist "%XAMPP_HTDOCS%" (
                    rmdir /S /Q "%XAMPP_HTDOCS%"
                )
                mkdir "%XAMPP_HTDOCS%"
                xcopy "%PROJECT_SOURCE%\\*" "%XAMPP_HTDOCS%\\" /E /I /Y
                '''
            }
        }

        stage('Open Website in Browser') {
            steps {
                echo '🌐 Opening website in default browser...'
                bat 'start "" "http://localhost/EventProject/index.php"'
            }
        }
    }

    post {
        always {
            echo '✔️ Pipeline execution complete.'
        }
        success {
            echo '✅ Build completed successfully!'
        }
        failure {
            echo '❌ Build failed!'
        }
    }
}
